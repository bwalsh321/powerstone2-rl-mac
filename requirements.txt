# Power Stone 2 RL — Python deps, pinned to the working M2 venv (Sep 2026).
# Python 3.11. The libretro harness itself (`_retro`) is NOT a pip package —
# it's built from the sdlarch-rl fork (see README "Build the harness").

gym==0.26.2              # the env speaks the old-gym 4-tuple API
gymnasium==1.3.0         # sb3's side of the fence
shimmy>=2.0              # lets sb3 2.x wrap the old-gym env
stable-baselines3==2.9.0
torch==2.13.0
numpy==2.4.6
pygame==2.6.1            # make_savestates / watch_play windows
tensorboard==2.21.0

# Optional — live audio in watch_play.py (warns and runs silent without it):
# sounddevice
